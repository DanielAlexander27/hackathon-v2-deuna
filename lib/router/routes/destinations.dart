import 'route.dart';

class PrincipalDestinations {
  static final home = Route(path: '/home');
  static final wallet = Route(path: '/wallet');
  static final transactions = Route(path: '/transactions');
  
}
